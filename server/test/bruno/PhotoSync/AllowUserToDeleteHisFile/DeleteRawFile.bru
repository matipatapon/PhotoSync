meta {
  name: DeleteRawFile
  type: http
  seq: 10
}

delete {
  url: {{HOST}}/file?id=1
  body: none
  auth: inherit
}

params:query {
  id: 1
}

headers {
  Authorization: {{TOKEN}}
}

assert {
  res.status: eq 200
  res.body: eq
}

settings {
  encodeUrl: true
}
