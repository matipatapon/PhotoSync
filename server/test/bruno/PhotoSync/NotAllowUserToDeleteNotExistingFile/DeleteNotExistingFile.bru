meta {
  name: DeleteNotExistingFile
  type: http
  seq: 5
}

delete {
  url: {{HOST}}/file?id=2
  body: none
  auth: inherit
}

params:query {
  id: 2
}

headers {
  Authorization: {{TOKEN}}
}

assert {
  res.status: eq 404
  res.body: eq
}

settings {
  encodeUrl: true
}
