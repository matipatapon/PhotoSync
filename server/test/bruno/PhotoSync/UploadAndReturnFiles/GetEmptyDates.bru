meta {
  name: GetEmptyDates
  type: http
  seq: 20
}

get {
  url: {{HOST}}/dates?year=2022&month=05
  body: none
  auth: inherit
}

params:query {
  year: 2022
  month: 05
}

headers {
  Authorization: {{TOKEN}}
}

assert {
  res.headers["access-control-allow-origin"]: eq {{ALLOWED_ORIGIN}}
  res.status: eq 200
}

tests {
  test("should return proper dates", function(){
    expect(res.getBody()).to.eql(
      [
      ])
  })
}

settings {
  encodeUrl: true
}
