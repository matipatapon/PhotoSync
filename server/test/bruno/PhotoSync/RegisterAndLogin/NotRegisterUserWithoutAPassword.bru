meta {
  name: NotRegisterUserWithoutAPassword
  type: http
  seq: 8
}

post {
  url: {{HOST}}/register
  body: json
  auth: inherit
}

body:json {
  {
    "username": "userwithoutapassword"
  }
  
}

assert {
  res.status: eq 400
  res.body: eq ""
}
