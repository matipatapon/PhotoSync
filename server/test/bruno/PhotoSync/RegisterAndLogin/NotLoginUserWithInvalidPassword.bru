meta {
  name: NotLoginUserWithInvalidPassword
  type: http
  seq: 9
}

post {
  url: {{HOST}}/login
  body: json
  auth: inherit
}

assert {
  res.status: eq 401
  res.body: eq ""
}

script:pre-request {
  var username = bru.getCollectionVar("USERNAME")
  req.setBody(`{"username": "${username}", "password": "invalidpassword"}`)
}
