meta {
  name: NotRegisterExistingUser
  type: http
  seq: 3
}

post {
  url: {{host}}/register
  body: json
  auth: inherit
}

assert {
  res.status: eq 400
  res.body: eq ""
}

script:pre-request {
  var username = bru.getCollectionVar("username")
  var password = bru.getCollectionVar("password")
  req.setBody(`{"username": "${username}", "password": "${password}"}`)
}
