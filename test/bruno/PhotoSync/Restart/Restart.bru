meta {
  name: Restart
  type: http
  seq: 2
}

post {
  url: {{host}}/exit
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

script:post-response {
  await new Promise(r => setTimeout(r, 2000));
}
