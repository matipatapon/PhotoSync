meta {
  name: NotRegisterUserWithoutAPassword
  type: http
  seq: 7
}

post {
  url: {{host}}/register
  body: json
  auth: inherit
}

body:json {
  {
    "username": "userwithoutapassword"
  }
  
}

assert {
  res.status: eq 400
  res.body: eq ""
}
