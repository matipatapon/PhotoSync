meta {
  name: NotLoginUserWithoutPassword
  type: http
  seq: 9
}

post {
  url: {{host}}/login
  body: json
  auth: inherit
}

assert {
  res.status: eq 400
  res.body: eq ""
}

script:pre-request {
  var username = bru.getFolderVar("username")
  req.setBody(`{"username": "${username}"`)
}
