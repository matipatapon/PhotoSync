meta {
  name: NotAllowToGetFileWhenTokenIsInvalid
  type: http
  seq: 1
}

get {
  url: {{HOST}}/file?id=1
  body: none
  auth: inherit
}

params:query {
  id: 1
}

headers {
  Authorization: {{INVALID_TOKEN}}
}

assert {
  res.status: eq 403
  res.body: 
}

settings {
  encodeUrl: true
}
