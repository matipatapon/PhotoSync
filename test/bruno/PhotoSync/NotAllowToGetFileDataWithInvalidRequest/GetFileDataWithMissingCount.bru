meta {
  name: GetFileDataWithMissingCount
  type: http
  seq: 5
}

get {
  url: {{HOST}}/file_data?offset=0
  body: none
  auth: inherit
}

params:query {
  offset: 0
}

headers {
  Authorization: {{TOKEN}}
}

assert {
  res.status: eq 400
  res.body: 
}

settings {
  encodeUrl: true
}
