meta {
  name: GetFileDataWithMissingOffset
  type: http
  seq: 7
}

get {
  url: {{HOST}}/file_data?count=10
  body: none
  auth: inherit
}

params:query {
  count: 10
}

headers {
  Authorization: {{TOKEN}}
}

assert {
  res.status: eq 400
  res.body: 
}

settings {
  encodeUrl: true
}
